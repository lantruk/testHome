/*
import {LOAD_DASHBOARDDIARY_DATA, LOAD_CONSUMED_DATA} from '../constants.js'
import { dateToUrl } from '../utils'


export function loadDashboardDiaryData(date, opt) {

    return {
        type: LOAD_DASHBOARDDIARY_DATA,
        callDashboardDiary: 'api/v1/user_statistic/' + dateToUrl(date),
        ...opt
            //'api/v1/user_statistic/' + dateToUrl(date),
            // должны сформировать:
            //'api/v1/user_statistic/2017-01-23T15:16:00+03:00
    }
}

export function LoadConsumedFood(date, opt) {

    return {
        type: LOAD_CONSUMED_DATA,
        callConsumedFood: 'api/v1/consumed_food/?date=' + dateToUrl(date),
        ...opt
    }
}


*/

